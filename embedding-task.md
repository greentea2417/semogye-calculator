# 세모계 지식 임베딩 및 반복 실행 과제

## 목적
세모계의 사업 정보, 계산기 제작 원칙, SEO 기준, 수익성 평가 기준을 임베딩하여 ChromaDB에 저장하고, 저장된 지식을 검색해 신규 계산기 후보 1개를 평가하는 반복 루프를 구축한다.

이번 작업에서는 실제 계산기 구현과 배포는 하지 않는다. 지식 저장, 검색 검증, 후보 평가 루프, 완료 보고 장치까지만 진행한다.

## 저장 위치
- 임베딩 대상 원문:
  - /home/ubuntu/work/semogye-calculator/task.md
  - /home/ubuntu/work/semogye-calculator/REVIEW.md
  - /home/ubuntu/work/semogye-calculator/README.md
- 벡터 DB:
  - /home/ubuntu/work/semogye-calculator/.knowledge-index/
- 중복 방지:
  - SHA256 해시

## 절대 규칙
- API 키, 비밀번호, 토큰, 개인키, 개인정보는 임베딩하거나 로그에 출력하지 않는다.
- 원본 파일은 삭제하거나 변경하지 않는다.
- 기존 DB가 있으면 덮어쓰기 전에 백업한다.
- 확인되지 않은 정보를 임의로 만들어 넣지 않는다.
- 실제 실행 전 파일 목록과 구현 계획을 먼저 보고하고 승인받는다.
- app/**/*.tsx 전체는 1차 임베딩에 포함하지 않는다.
- 1차 검색 검증이 통과한 뒤 필요할 때만 범위를 확대한다.

## 사전 보고 항목
실제 임베딩 전에 다음을 먼저 보고한다.
- 임베딩 대상 파일 수
- 총 문자 수 또는 예상 토큰 수
- 예상 API 비용
- 민감정보 검사 결과
- 설치할 패키지 목록
- OPENAI_API_KEY 실제 호출 가능 여부

## 반복 루프

### 1단계. 대상 문서 수집
- task.md, REVIEW.md, README.md를 읽는다.
- 민감정보 포함 여부를 검사한다.
- 민감정보가 있으면 해당 구간은 제외하거나 마스킹한다.

### 2단계. 문서 분할
- 의미 단위로 chunk를 나눈다.
- 각 chunk에 source, title, category, created_at 메타데이터를 붙인다.
- chunk는 중복 의미를 최소화하도록 만든다.

### 3단계. 임베딩
- OpenAI `text-embedding-3-small` 모델을 사용한다.
- 임베딩 실패 chunk는 재시도하되, 같은 chunk를 중복 저장하지 않는다.
- 문서 해시는 SHA256으로 계산하고 동일 문서는 재임베딩하지 않는다.

### 4단계. 벡터 DB 저장
- ChromaDB에 저장한다.
- 저장 경로는 /home/ubuntu/work/semogye-calculator/.knowledge-index/ 이다.
- 기존 인덱스가 있으면 백업 후 반영한다.

### 5단계. 저장 검증
- 저장된 문서 수를 확인한다.
- 실패 건수를 확인한다.
- 샘플 chunk가 정상 검색되는지 확인한다.

### 6단계. 검색 테스트
세모계 관련 질문 최소 3개로 검색한다.
예:
- 세모계의 계산기 제작 원칙은 무엇인가?
- 세모계에서 SEO 페이지를 만들 때 우선순위는 무엇인가?
- 세모계의 수익성 높은 계산기 기준은 무엇인가?

검색 결과가 원문과 일치하는지 검증한다.

### 7단계. 후보 평가
검색된 지식을 바탕으로 신규 계산기 후보 3개를 평가한다.
- 후보별 장점/단점
- 공식 근거 존재 여부
- 검색 수요
- 수익성
- 세모계 적합성

그중 1개를 최종 선정한다.

### 8단계. embedding-REVIEW.md 검수
embedding-REVIEW.md 기준으로 검사한다.
FAIL이 있으면 실패 항목만 수정한다.
최대 3회 반복한다.

### 9단계. 완료 보고 장치
- delegate_task를 우선 사용한다.
- 불가능하면 watchdog/finalizer 방식으로 완료·실패·중단 시 텔레그램 최종 보고를 보장한다.
- 작업 ID, 시작 시각, 현재 상태를 기록한다.
- 무응답 종료를 금지한다.

## 중단 조건
- OPENAI API 키 호출 불가
- ChromaDB 설치 또는 저장 실패
- 민감정보 제거 불가
- 검색 결과가 원문과 불일치
- 후보 평가 근거 부족
- embedding-REVIEW.md 기준 FAIL이 3회 후에도 해소되지 않음

## 최종 보고 형식
[상태]
[임베딩한 자료]
[저장 건수]
[제외한 민감정보]
[사용 모델]
[DB 위치]
[검색 테스트 결과]
[선정한 계산기 후보]
[반복 횟수]
[API 사용량/비용]
[완료 알림 검증]
[다음 단계]
