# 세모계 지식 임베딩 · 검색 검증 · 후보 평가 결과 보고서

- 작업 ID: `embed-20260710-150008`
- 기준 문서: `embedding-task.md`, `embedding-REVIEW.md`
- 최종 상태: **완료**
- 수정 반복 횟수: **3회** (허용 한도 3회 내)

---

## 1. 임베딩

| 항목 | 값 |
|---|---|
| 모델 | `text-embedding-3-small` (1,536차원) |
| 대상 문서 | `task.md`, `REVIEW.md`, `README.md` (3개) |
| 총 문자 수 | 10,795자 |
| 생성 chunk | 48개 (task.md 28 / REVIEW.md 16 / README.md 4) |
| 저장 건수 | 48 |
| 실패 건수 | 0 |
| 벡터 DB | ChromaDB 1.5.9, `.knowledge-index/`, cosine |
| 중복 방지 | chunk 텍스트 SHA256 = id, 문서 SHA256 = manifest |
| 임베딩 레시피 버전 | `v3-h3-headings` |

`app/**/*.tsx`는 1차 대상에서 제외했습니다. 원본 `task.md`·`REVIEW.md`·`README.md`는 읽기만 했고 수정하지 않았습니다. 기존 인덱스는 매 실행 전 `.knowledge-index.bak.<타임스탬프>/`로 백업했습니다.

### 증분 재임베딩 검증

동일 문서로 재실행 시 **신규 0건 / 건너뜀 48건 / 토큰 0**이 나오는 것을 실제로 확인했습니다. 변경된 문서만 재처리됩니다.

### 민감정보

실제 자격증명 **값** 패턴(OpenAI 키, GitHub PAT, AWS 키, private key, bearer 토큰, 대입형 secret, 이메일, 전화번호, 주민번호)으로 전수 검사 → **0건**. 따라서 제외한 문서·구간 없음.

`task.md`에 등장하는 `API 키·secret 노출 금지` 같은 표현은 **비밀값이 아니라 정책 문구**이므로 통과시켰습니다. 초기 검사기는 `secret`이라는 단어만 보고 오탐을 냈고, 값 패턴을 보도록 고쳤습니다. 키 값은 이 보고서, 로그, 임베딩 어디에도 포함되지 않습니다.

---

## 2. 검색 검증 (질의 3건)

판정 기준 두 가지를 모두 적용했습니다.

1. **grounded** — 검색된 텍스트가 원본 파일에 실제로 존재하는가
2. **recall@3** — 정답 섹션이 상위 3건에 들어오는가

| # | 질의 | 최상위 결과 | grounded | recall@3 |
|---|---|---|---|---|
| 1 | 세모계의 계산기 제작 원칙은? | `task.md / 1단계. 기존 세모계 현황 확인` | ✅ | ✅ |
| 2 | SEO 페이지를 만들 때 확인할 것은? | `task.md / 10단계. SEO 설계` | ✅ | ✅ |
| 3 | 수익성 높은 계산기를 고르는 기준은? | `task.md / 5단계. 수익성 검사` | ✅ | ✅ |

**최종: grounded 9/9, recall@3 3/3 → PASS**

---

## 3. 수정 반복 기록 (3회)

검색 검증을 처음 돌렸을 때 3번 질의가 **오답**이었습니다. "수익성 기준"을 물었는데 `5. 기능 검수` 체크리스트가 1위로 올라왔습니다. 세 번에 걸쳐 원인을 좁혔습니다.

### Iteration 1 — 문맥 없는 chunk

- **실패 항목**: 검색 정확성 (질의 3)
- **원인**: chunk가 `- CPC가 기대되는 분야인가` 같은 맨 체크리스트여서 어느 문서·주제인지 신호가 없었음. 반면 `기능 검수` chunk는 "계산"을 4번 반복해 질의의 "계산기"와 어휘가 겹침.
- **수정**: 임베딩 입력에 `[문서: ...][섹션: ...]` 문맥 헤더를 붙이는 contextual retrieval 적용. 저장되는 원문(document)은 그대로 유지. 부수적으로 OpenAI 응답을 `index` 기준 정렬해 벡터-문서 정렬 가정을 제거.
- **재검사**: 질의 1·2 개선(0.56→0.70, 0.55→0.57), **질의 3 여전히 FAIL**

### Iteration 2 — 잘못된 섹션 라벨

- **실패 항목**: 검색 정확성 (질의 3)
- **원인**: H2 블록을 H3로 쪼갤 때 heading을 상위 H2에서 그대로 물려받아, `### 5단계. 수익성 검사` chunk가 `반복 루프`로 라벨링됨. Iteration 1이 붙인 문맥 헤더에 **틀린 섹션명**이 주입되고 있었음.
- **수정**: H3 분할 시 각 chunk가 자기 H3 제목을 갖도록 청킹 수정. 함께, 재임베딩이 실제로 일어나지 않았는데도 manifest에 새 레시피 버전이 기록돼 재구축 가드가 무력화되던 버그도 수정(성공 시에만 버전 기록).
- **재검사**: 섹션 라벨 정상화, **질의 3 여전히 FAIL** (dense 점수만으로는 못 이김)

### Iteration 3 — 한국어 조사

- **실패 항목**: 검색 정확성 (질의 3)
- **원인**: dense 점수를 보정하려 어휘 중첩 신호를 넣었으나 어절 단위 토큰화가 한국어에서 동작하지 않음. 질의 토큰은 `수익성이`, 헤딩은 `수익성` → 정확 일치 실패로 lexical 점수가 0.
- **수정**: 문자 bigram 중첩으로 교체(형태소 분석기 없이 어간 공유를 포착). 최종 랭킹 = `0.75 × cosine + 0.25 × lexical`, 헤딩 중첩에 가중.
- **재검사**: 질의 3 최상위가 `5단계. 수익성 검사`로 교정. **recall@3 3/3, PASS**

---

## 4. 계산기 후보 3개 평가

평가 기준은 **임의로 만들지 않고 벡터 DB에서 검색해 가져온 원문**을 근거로 적용했습니다 (`task.md / 3단계. 후보 1차 제외`, `5단계. 수익성 검사`, `7단계. 공식 계산 근거 검증`).

기존 계산기 실측: `salary, hourly, hourly-multi, bonus, burden, vat, profit, compare, freelance, owner-cost-mvp, retirement, annual, unemployment, life/{bmi,body-age,grade,kbm,waste-time}`

| 후보 | 중복 | 공식 근거 | 수익성 | 판정 |
|---|---|---|---|---|
| A. 4대보험료 계산기 | ⚠️ 부분 중복 | 강함 (법령·공단 고시) | A | **보류** |
| B. 종합소득세 계산기 | 없음 | 강함 (국세청) | A | **제외** |
| C. 대출 원리금 상환 계산기 | 없음 | 명확·검증가능 | A | **선정** |

### A. 4대보험료 계산기 — 보류

`app/utils/salaryCalculators.ts`가 **이미 국민연금·건강보험·장기요양을 산출**하고 있고, `salary` 페이지가 항목별로 표시합니다. `task.md / 1단계`의 "기존 계산기와 동일하거나 지나치게 유사한 후보는 제외한다"에 걸립니다. 수익성 자체는 A급이라 폐기가 아닌 **보류**로 둡니다.

> **별건으로 발견한 위험**: 해당 파일의 요율이 `// 2024 4대보험 및 비과세 요율 설정`로 하드코딩돼 있습니다(국민연금 4.5%, 건강보험 3.545%, 장기요양 12.95%). 현재는 2026년이므로 **기존 급여 계산기의 정확성 자체가 의심**됩니다. 신규 계산기보다 이쪽 검증이 우선순위가 높다고 봅니다.

### B. 종합소득세 계산기 — 제외

`task.md / 3단계` 제외 조건 중 **"의료·법률·세무 판단을 단정적으로 제공해야 함"**에 정면으로 해당합니다. 공제 항목 조합에 따라 결과가 크게 갈리고, 오해 시 사용자 피해가 큽니다. 검색 수요와 CPC는 최상위지만 규칙상 제외합니다.

### C. 대출 원리금 상환 계산기 — **선정**

- **중복 없음**: 세모계에 금융 카테고리 계산기가 전무
- **계산식**: 원리금균등상환 `M = P·i(1+i)ⁿ / ((1+i)ⁿ − 1)` — 정의가 단일하고 수기·별도 코드로 교차검증 가능. 세율·법령처럼 매년 바뀌지 않아 유지보수 위험이 낮음
- **수익성 A 근거** (`5단계` 항목 대조): 금융 고가치 키워드 / 광고 친화 / 애드센스 안전 / 상환 스케줄 표로 체류시간 확보 / 관련 계산기 3개 이상 확장(원금균등, 중도상환수수료, DSR) / `salary`·`profit`과 내부링크 허브 / PDF 상환표 저장으로 유료 기능 연결 가능
- **예상 키워드**: 대출 이자 계산기, 원리금균등상환 계산기, 월 상환액 계산, 주택담보대출 이자 계산

**주의**: `7단계. 공식 계산 근거 검증`(기준일·반올림·예외조건 확정)은 **구현 착수 시점에 수행할 단계**이며, 이번 과제 범위(지식 임베딩 + 후보 선정)에는 포함되지 않았습니다. 이 선정은 후보 평가 수준의 근거이며, 구현 전 정식 검증이 필요합니다.

---

## 5. `embedding-REVIEW.md` 항목별 판정

| # | 검수 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | 대상 파일 수 보고 | PASS | 3개 |
| 1 | 문자 수/토큰 수 보고 | PASS | 10,795자 |
| 1 | 3개 문서만 1차 대상 | PASS | — |
| 1 | `app/**/*.tsx` 미포함 | PASS | — |
| 2 | API 키 미출력 | PASS | 존재 여부만 보고 |
| 2 | 비밀번호/토큰/개인키 미출력 | PASS | — |
| 2 | 개인정보 임베딩 제외 | PASS | 검출 0건 |
| 2 | 민감정보 검사 결과 기록 | PASS | 9개 패턴, 0건 |
| 3 | `text-embedding-3-small` 사용 | PASS | 1,536차원 응답 확인 |
| 3 | ChromaDB 저장 | PASS | 48 chunk |
| 3 | 저장 위치 명시 | PASS | `.knowledge-index/` |
| 3 | SHA256 중복 방지 | PASS | 재실행 신규 0건 |
| 3 | 변경 문서만 재임베딩 | PASS | 토큰 0 재실행 확인 |
| 3 | 기존 DB 백업 | PASS | `.knowledge-index.bak.*` |
| 4 | 질의 3건 이상 | PASS | 3건 |
| 4 | 검색 결과 원문 일치 | PASS | grounded 9/9 |
| 4 | 검색 정확성 검증 | PASS | recall@3 3/3 (3회 수정 후) |
| 4 | 잘못된 결과 수정 | PASS | Iteration 1~3 |
| 5 | 후보 3개 평가 | PASS | A/B/C |
| 5 | 공식 근거 있는 후보 우선 | PASS | — |
| 5 | 세모계 적합성 평가 | PASS | 중복 실측 대조 |
| 5 | 수익성/SEO/확장성 고려 | PASS | `5단계` 항목 대조 |
| 5 | 최종 후보 1개 선정 | PASS | 대출 원리금 상환 계산기 |
| 6 | FAIL 항목만 수정 | PASS | 검색 정확성만 |
| 6 | 매번 전체 재임베딩 안 함 | PASS | manifest 해시 |
| 6 | 최대 3회 반복 | PASS | 정확히 3회 |
| 7 | 작업 ID 기록 | PASS | `embedding-run-ledger.json` |
| 7 | 시작 시각 기록 | PASS | manifest `runs[]` |
| 7 | 현재 상태 기록 | PASS | ledger |
| 7 | 텔레그램 최종 보고 보장 | PASS | `finalize.py` |
| 7 | 완료/실패/중단 중 하나로 종결 | PASS | 완료 |

**FAIL 0건. 최종 판정: 완료**

---

## 6. API 사용량 / 비용

| 구분 | 토큰 | 비용(USD) |
|---|---|---|
| 연결 테스트 | 8 | $0.00000016 |
| 임베딩 (총 6회 실행, 3회 재구축 포함) | 39,304 | $0.000786 |
| 검색 질의 | 약 300 | $0.000006 |
| **합계** | **약 39,600** | **약 $0.0008** |

최종 인덱스 1회 구축분만 따지면 11,665토큰 / $0.00023입니다. 나머지는 3회 수정 루프에서 발생한 재구축 비용입니다.

---

## 7. 재현 방법

```bash
export OPENAI_API_KEY=...            # 값은 .env 에서 로드, 출력 금지
python scripts/embedding/ingest.py           # 증분 임베딩 (변경분만)
python scripts/embedding/ingest.py --force   # 전체 재구축
python scripts/embedding/query.py            # 검색 검증, PASS/FAIL 종료코드
python scripts/embedding/finalize.py --status 완료 --iterations 3 --note "..."
```

`finalize.py --wrap -- <명령>`으로 감싸면 자식 프로세스가 실패하거나 중단돼도 `finally`에서 텔레그램 보고가 나갑니다.

---

## 8. 남은 위험과 다음 단계

1. **`salaryCalculators.ts`의 2024년 요율 하드코딩** — 신규 계산기보다 우선 검증 필요
2. 하이브리드 랭킹 가중치(0.75/0.25)는 질의 3건으로만 확인 — 질의 세트를 늘려 재조정 권장
3. `.knowledge-index/`는 바이너리 인덱스라 `.gitignore` 처리, 스크립트로 재현
4. 선정된 대출 계산기의 `7단계. 공식 계산 근거 검증`은 구현 착수 시 수행
5. 검색 검증 통과했으므로 `app/**/*.tsx`로 임베딩 범위 확대 가능
