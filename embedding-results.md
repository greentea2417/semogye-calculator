# 세모계 지식 임베딩 · 후보 평가 결과

- 작업 ID: `embed-20260710-145756`
- 시작 시각(UTC): 2026-07-10T14:57:56Z
- 상태: 완료
- 반복 횟수: 3회 (manifest `runs` 기준 — ①최초 임베딩 48 chunk ②동일 문서 재실행 시 0 chunk로 중복 방지 동작 확인 ③`EMBED_VERSION=v2-contextual`로 청킹 교체 후 재구축 48 chunk)

## 1. 임베딩 결과

| 항목 | 값 |
|---|---|
| 모델 | `text-embedding-3-small` (1536차원) |
| 벡터 DB | ChromaDB 1.5.9, collection `semogye-knowledge` |
| 저장 위치 | `.knowledge-index/` |
| 대상 문서 | `task.md`, `REVIEW.md`, `README.md` (3개) |
| 총 문자 수 | 10,795자 |
| 저장 chunk 수 | 48 (task 28 / REVIEW 16 / README 4) |
| 실패 건수 | 0 |
| 사용 토큰 | 8,140 (임베딩) + 167 (검색 질의) |
| 비용 | 약 $0.000166 |
| 민감정보 적출 | 0건 (제외 문서 0개) |
| 중복 방지 | chunk 텍스트 SHA256 = id, 문서 SHA256 = manifest 기록 |

`app/**/*.tsx`는 1차 임베딩 대상에서 제외했다. 원본 `task.md` / `REVIEW.md` / `README.md`는 읽기만 했고 수정하지 않았다.

민감정보 스캐너는 자격증명의 **값**만 잡는다(`sk-…`, `ghp_…`, `AKIA…`, `BEGIN PRIVATE KEY`, 이메일, 주민번호, 휴대폰). 정책 문서에 나오는 "API 키를 노출하지 말 것" 같은 **단어**는 값이 아니므로 통과시킨다.

## 2. 검색 검증

질의 6건(기본 3 + 평가용 3) 모두 `all_hits_grounded_in_source: true`. 검색된 모든 chunk의 첫 줄이 해당 원본 파일에 문자열 그대로 존재함을 확인했다.

| 질의 | 1순위 히트 | 유사도 |
|---|---|---|
| 세모계의 계산기 제작 원칙은 무엇인가? | `task.md :: 사용 원칙` | 0.5614 |
| SEO 페이지를 만들 때 무엇을 확인해야 하는가? | `task.md :: 10단계. SEO 설계` | 0.5616 |
| 수익성이 높은 계산기를 고르는 기준은? | `REVIEW.md :: 기능 검수` | 0.4626 |
| 후보 정량 평가 항목은? | `task.md :: 4단계. 후보 정량 평가` | 0.6096 |
| 수익성 등급 A~D 분류는? | `task.md :: 5단계. 수익성 검사` | 0.4214 |
| 공식 근거 출처 우선순위는? | `task.md :: 7단계. 공식 계산 근거 검증` | 0.6425 |

3번째 질의는 1순위가 「기능 검수」로 빗나갔고 2순위에서 「REVIEW.md 수익성 검수」가 잡혔다. 원문 근거는 유지되므로 오답 제외 대상은 아니지만, 상위 정확도가 낮은 유일한 질의다.

## 3. 후보 평가 (검색된 기준을 근거로)

평가 기준은 벡터 DB에서 검색한 `task.md :: 4단계(20개 항목 × 10점)`과 `5단계(수익성 등급 A~D)`를 그대로 적용했다.

기존 라우트(`salary`, `hourly`, `hourly-multi`, `bonus`, `burden`, `vat`, `profit`, `compare`, `freelance`, `owner-cost-mvp`, `life/bmi`, `life/body-age`, `life/grade`, `life/kbm`, `life/waste-time`, `retirement`, `annual`, `unemployment`)와의 중복을 먼저 확인했다.

| 후보 | 공식 근거 | 정량 점수 | 수익성 등급 | 중복 위험 |
|---|---|---|---|---|
| **A. 대출 원리금균등상환 계산기** | 원리금균등상환 상각식 (수학적 정의, 제도 변경 무관) | **165 / 200** | **A** | 없음 |
| B. 4대보험료 계산기 | 국민연금공단·건강보험공단·근로복지공단 고시 요율 | 163 / 200 | A | **있음** — `app/utils/salaryCalculators.ts`가 이미 4대보험 공제를 내부 계산 |
| C. 자동차 취득세 계산기 | 지방세법 제12조 세율 | 148 / 200 | B | 없음 |

세 후보 모두 「공식 계산 근거」 항목이 7점 이상이라 자동 제외 대상은 없다.

### 최종 선정: 대출 원리금균등상환 계산기

- **점수** 165점 (우선 제작 후보 기준선 160점 상회)
- **수익성 등급** A
- **선정 이유**
  - 계산식이 수학적으로 확정되어 있어 법령·세율 개정 위험이 사실상 0이다. 세모계의 "가장 신뢰할 수 있는 계산기" 원칙과 가장 잘 맞는다.
  - 금융 키워드라 CPC가 높고 광고 친화적이며, 애드센스 정책상 위험이 없다.
  - 기존 16종과 중복이 전혀 없고, 세모계에 아직 없는 **금융 축**을 새로 연다.
  - 관련 계산기 3개 이상으로 확장 가능: 중도상환수수료 · 거치기간 이자 · DSR/DTI. 금융 카테고리의 내부링크 허브가 된다.
- **탈락 사유**
  - B(4대보험)는 점수는 근접하지만 급여 계산기와 로직이 겹쳐 `task.md` 3단계 "기존 계산기와 중복" 제외 조건에 걸릴 소지가 있고, 요율이 매년 고시되어 유지보수·기준일 리스크가 크다.
  - C(자동차 취득세)는 반복 사용 빈도가 낮고(차량 구매 시 1회) 점수가 보완 검토 구간(140~159)에 머문다.
- **예상 주요 키워드** 대출 이자 계산기, 원리금균등상환 계산기, 월 상환액 계산, 주택담보대출 이자 계산
- **예상 사용자** 주택·전세 대출 실행 예정자, 신용대출 비교 검토자, 자영업 운전자금 대출 검토자

## 4. 남아 있는 위험요소

- **검색 수요 수치는 실측이 아니다.** 이번 반복에서는 키워드 도구/웹 조사를 다시 돌리지 않았다(반복 규칙: 실패 항목만 재실행). 위 점수 중 「검색 수요」·「CPC」는 정성 판단이며, 구현 착수 전 `task.md` 2단계로 한 번 실측을 붙여야 한다.
- `.knowledge-index.bak.*` 백업 디렉터리가 6개 누적됐다. 삭제는 승인 대상이라 손대지 않았고, `.gitignore`로 커밋에서만 제외했다.

## 5. 다음 단계

이번 과제 범위는 지식 임베딩·검색 검증·후보 선정까지다. 실제 구현은 하지 않았다. 착수 시 `task.md` 7단계(공식 근거 검증)부터 이어가면 된다.
